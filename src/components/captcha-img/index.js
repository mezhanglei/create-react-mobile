import styles from './index.less';
import React from "react";

/**
 * 拖拽图形验证码
 */
export default class extends React.Component {
    constructor(props) {
        super(props);
        this.state = {};
    }
    static defaultProps = {
    }

    render() {
        return (
            
        );
    }
}
